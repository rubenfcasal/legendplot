% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sshade3d.R
\name{vb2tri3d}
\alias{vb2tri3d}
\title{Value per triangle from values at vertices}
\usage{
vb2tri3d(x, s)
}
\arguments{
\item{x}{triangular mesh (\code{mesh3d} object with an \code{it} component, see
\code{\link[rgl:mesh3d]{rgl::mesh3d()}}).}

\item{s}{vector of values associated with the vertices of \code{x} (same
length as the number of columns of \code{x$vb}).}
}
\value{
Numeric vector with one value per triangle (column of \code{x$it}), equal to
the average of the values of \code{s} at its vertices.
}
\description{
For each triangle of a mesh, computes the average of the values
associated with its three vertices. This is useful for going from a
per-vertex value to a per-face value (for example, to use
\code{meshColor = "faces"} in \code{\link[rgl:shade3d]{rgl::shade3d()}} starting from a quantity
defined at the vertices).
}
\examples{
library(rgl)
open3d() # Alternatively, use `new3d()` to clear the current device or open a new one
z_tri <- vb2tri3d(volcanom, volcanom$vb[3, ])
shade3d(volcanom, col = scolor(z_tri, col = terrain.colors(128)))

}
\seealso{
\code{\link[=sshade3d]{sshade3d()}}
}
