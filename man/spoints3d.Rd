% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spoints3d.R
\name{spoints3d}
\alias{spoints3d}
\alias{spoints3d.default}
\title{3D scatter plot with a continuous color scale}
\usage{
spoints3d(x, ...)

\method{spoints3d}{default}(
  x,
  y = NULL,
  z = NULL,
  s,
  slim = range(s, finite = TRUE),
  col = jet.colors(128),
  xlab = NULL,
  ylab = NULL,
  zlab = NULL,
  type = "p",
  legend = TRUE,
  legend.zoom = 0.4,
  legend.width = 0.1,
  legend.dim = 0.2,
  legend.lab = NULL,
  box = TRUE,
  lab.breaks = NULL,
  lab.ticksize = 0.5,
  lab.dist = 3,
  add = FALSE,
  ...
)
}
\arguments{
\item{x}{object used to select a method. In the default method, it provides the \code{x}
coordinates for the plot (and optionally the \code{y} and \code{z} coordinates;
any reasonable way of defining the coordinates is acceptable,
see the function \code{\link[=xyz.coords]{xyz.coords()}} for details).}

\item{...}{additional arguments passed to \code{\link[rgl:plot3d]{rgl::plot3d()}} for the main
scatter plot (e.g. \code{size}).}

\item{y, z}{\code{y} and \code{z} point coordinates. Alternatively, a single argument \code{x}
can be provided.}

\item{s}{vector used to color the points.}

\item{slim}{limits (vector of length 2 with the minimum and maximum) used
to set up the color scale.}

\item{col}{color table used to set up the color scale. Defaults to
\code{jet.colors(128)}.}

\item{xlab, ylab, zlab}{labels for the coordinates.}

\item{type}{character indicating the type of item to plot (see
\code{\link[rgl:plot3d]{rgl::plot3d()}}: \code{"p"} for points, \code{"s"} for spheres, \code{"l"} for lines,
\code{"h"} for line segments from z = 0, and \code{"n"} for nothing).}

\item{legend}{logical; if \code{TRUE} (default), the active rgl device is splitted
into two subscenes, drawing the main plot in one and the categorical
legend in the other (see \code{\link[=fplot3d]{fplot3d()}}).
if \code{FALSE} only the (coloured) main plot is drawn and the arguments related to
the legend are ignored (\code{\link[=fplot3d]{fplot3d()}} is not called).}

\item{legend.zoom}{zoom factor applied to the legend subscene (see
\code{\link[rgl:view3d]{rgl::view3d()}}).}

\item{legend.width}{relative width of the color bar, as a fraction of
the corresponding dimension of the legend subscene/panel.}

\item{legend.dim}{relative dimension of the legend panel, as a fraction of
the full width (or height) of the device.}

\item{legend.lab}{legend title.}

\item{box}{logical; if \code{TRUE} (the default) a rectangle is drawn around
the color bar.}

\item{lab.breaks}{labels for the legend axis breaks; if \code{NULL} they are
generated automatically (see \code{\link[=axis3]{axis3()}}).}

\item{lab.ticksize}{length of the legend axis tick marks, as a fraction
of the distance to the center of the legend panel.}

\item{lab.dist}{distance between the tick marks and the labels of the
legend axis, as a multiple of \code{lab.ticksize}.}

\item{add}{logical; if \code{TRUE} the scatter plot is just added to the existing
plot (including the legend if \code{legend = TRUE}).}
}
\value{
Called for its side effect (draws the 3D scatter plot and, unless
\code{legend = FALSE}, the legend on the active rgl device).
}
\description{
A generic function that, by default, draws a 3D scatter plot (\code{\link[rgl:plot3d]{rgl::plot3d()}})
with points colored according to a continuous scale associated with a vector
of values \code{s}, and (optionally) adds a color-bar legend (via \code{\link[=splot3d]{splot3d()}}).
}
\examples{
library(rgl)
open3d() # Alternatively, use `new3d()` to clear the current device or open a new one
with(mtcars, spoints3d(hp, qsec, wt, s = mpg, type = "s"))

}
\seealso{
\code{\link[=fpoints3d]{fpoints3d()}}, \code{\link[=splot3d]{splot3d()}}, \code{\link[=scolor]{scolor()}}, \code{\link[=sshade3d]{sshade3d()}}, \code{\link[rgl:plot3d]{rgl::plot3d()}}.
}
